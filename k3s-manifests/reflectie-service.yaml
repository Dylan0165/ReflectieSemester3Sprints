apiVersion: v1
kind: Service
metadata:
  name: reflectie
  namespace: reflectie
  labels:
    app: reflectie
    tier: web
spec:
  selector:
    app: reflectie
    tier: web
  ports:
    - name: http
      port: 2001
      targetPort: 80